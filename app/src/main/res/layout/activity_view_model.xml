<layout
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="viewModel"
            type="com.demo.aac.view_model.MyViewModel" />

        <variable
            name="presenter"
            type="com.demo.aac.data_binding.Presenter" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#FFF"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{viewModel.liveData,default=姓名}"
            android:textColor="#666"
            android:textSize="20sp" />

        <EditText
            android:id="@+id/edit_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:afterTextChanged="@{presenter.afterTextChanged}"
            android:text="@={viewModel.liveData}"
            android:textColor="#666"
            android:textSize="20sp" />
    </LinearLayout>
</layout>